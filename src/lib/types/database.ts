/**
 * Placeholder — à remplacer par le fichier généré automatiquement :
 *
 *   npx supabase gen types typescript --project-id <id-projet> > src/lib/types/database.ts
 *
 * À régénérer après chaque migration appliquée dans supabase/migrations/.
 */
export type Database = {
  public: {
    Tables: Record<string, { Row: Record<string, unknown> }>;
  };
};
